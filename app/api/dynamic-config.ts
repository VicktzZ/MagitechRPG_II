/**
 * Configuração para forçar rotas dinâmicas
 * Adicione esta linha no topo de TODAS as rotas API que usam Firebase:
 * 
 * export const dynamic = 'force-dynamic'
 * 
 * Isso previne o erro de pre-render durante o build
 */

export const dynamic = 'force-dynamic'
